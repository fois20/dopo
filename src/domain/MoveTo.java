/**
 * This enum indicates the directions to which the player wants to move, once again
 * i think this is better than having just a random number or anything as volatitle
 * as a number
 * 
 * @author juand
 */
package domain;

public enum MoveTo
{
	ABOVE,
	BELLOW,
	LEFT,
	RIGHT
}
